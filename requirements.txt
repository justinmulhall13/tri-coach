# Backend
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
python-dotenv>=1.0,<2

# Garmin (direct integration — reused, validated path)
garminconnect>=0.3,<1
curl_cffi>=0.7,<1

# Google Calendar (direct API)
google-api-python-client>=2.100,<3
google-auth>=2.30,<3
google-auth-oauthlib>=1.2,<2

# Coach agent
anthropic>=0.40,<1

# Web push notifications (proactive nudges)
pywebpush>=1.14,<3
